name: CI

on:
  push:
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      # 1) Lint first (fail-fast)
      - name: Lint
        run: npm run lint

      # 2) Install Playwright browsers (and OS deps)
      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      # 3) Run tests
      - name: Run Playwright tests
        run: npm test

      # 4) Upload artifacts even if tests fail
      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results


